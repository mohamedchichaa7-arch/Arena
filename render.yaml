services:
  - type: web
    name: game-arena
    runtime: node
    buildCommand: npm install && pip3 install --user --break-system-packages yt-dlp
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
