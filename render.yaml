services:
  - type: web
    name: game-arena
    runtime: node
    buildCommand: npm install && pip3 install --break-system-packages --target ./py-deps yt-dlp
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
